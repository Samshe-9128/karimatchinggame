export function Switch({checked,onCheckedChange,className="",...props}){return <button type="button" role="switch" aria-checked={checked} className={`inline-flex h-7 w-12 shrink-0 items-center rounded-full bg-surface-2 shadow-[var(--shadow-border)] transition-colors ${checked?"bg-accent":""} ${className}`} onClick={()=>onCheckedChange?.(!checked)} {...props}><span className={`block size-5 rounded-full bg-surface shadow-sm transition-transform ${checked?"translate-x-6":"translate-x-1"}`}/></button>}
